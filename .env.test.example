# Copy to .env.test (gitignored). Used only by Playwright authenticated specs.
# Use a dedicated Clerk user with email + password (not Google-only).
TEST_USER_EMAIL=
TEST_USER_PASSWORD=

# Optional: Vercel *preview* URL or local. Production hosts are refused
# (www.motamaati.in, motamaati.in, kharch-baant.vercel.app).
# PLAYWRIGHT_BASE_URL=http://localhost:3000
